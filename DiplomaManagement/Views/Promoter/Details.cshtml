@* @model DiplomaManagement.Models.DetailsViewModel<DiplomaManagement.Entities.Promoter> *@
@model DiplomaManagement.Entities.Promoter
@{
    ViewData["Title"] = "Details";
}

<h1>@Localizer["details"]</h1>

<div>
    <h4>@Localizer["promoter-details"]</h4>
    <hr />
@*     @if (TempData[$"SuccessMessage_{User.Identity.Name}"] != null)
    {
        <div class="alert alert-success alert-dismissible fade show" role="alert" id="info-message">
            @TempData[$"SuccessMessage_{User.Identity.Name}"]
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    }
    @if (TempData[$"ErrorMessage_{User.Identity.Name}"] != null)
    {
        <div class="alert alert-danger alert-dismissible fade show" role="alert" id="info-message">
            @TempData[$"ErrorMessage_{User.Identity.Name}"]
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    } *@
    <div asp-validation-summary="All" class="text-danger"></div>
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.User.FirstName)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.User.FirstName)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.User.LastName)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.User.LastName)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.User.Email)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.User.Email)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.ThesisLimit)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.ThesisLimit)
        </dd>
        <dt class = "col-sm-2">
            Assigned director
        </dt>
        <dd class="col-sm-10">
            @($"{Model.Director.User.FirstName} {Model.Director.User.LastName}")
        </dd>
    </dl>
</div>

<div>
    <a asp-action="Edit" asp-route-id="@Model?.PromoterUserId">@Localizer["edit-button"]</a> |
    <a asp-action="Index">@Localizer["back-to-list"]</a>
</div>